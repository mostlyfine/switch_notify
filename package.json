{
  "name": "switch_notify",
  "version": "1.0.0",
  "description": "",
  "main": "switch.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dryrun": "$(npm bin)/lambda-local -l switch.js -h check -e event-samples/test.json",
    "deploy": "$(npm bin)/sls deploy"
  },
  "keywords": [],
  "author": "mostlyfine",
  "license": "ISC",
  "dependencies": {
    "cheerio-httpcli": "^0.7.0"
  },
  "devDependencies": {
    "aws-sdk": "^2.79.0",
    "lambda-local": "^1.4.2",
    "serverless": "^1.16.1"
  }
}
